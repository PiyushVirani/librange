import { SectionRecord } from "./SectionRecord";

export interface SectionMatch extends SectionRecord {
  // Currently identical to SectionRecord.
  // Future fields could go here (confidence, notes, etc.)
}
